{
  "scripts": {
    "build": "tsc",
    "postinstall": "npm run build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "tsx": "^3.12.0"   // only if you intend to run TS directly in prod
  },
  "devDependencies": {
    "typescript": "^5.x"
  }
}
